#ifndef GETCH_H
#define GETCH_H

// return the character read after a keystroke immediately
// similar to the |getch(void)| defined in conio.h on Windows
extern char getch(void);
extern char getche(void);

#endif
